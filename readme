jquery를 사용할 때 custom.js script가 head안에 있을 경우엔
jquery 최상단에 

$(function(){

})

을 쓰고 그 안에 넣어야 html을 읽고 나서 jquery 실행함.
만약 그렇게 하지 않으려면 마감 body태그 윗 줄에 script 사용



toggle()은 show(), hide()가 같이 있는 것
근데 이건 순간적으로 펼쳤다 없어지기 때문에 너무 딱딱한 느낌

fadeToggle() 이나 slideToggle()을 사용하면 좀 더 좋음

eq()라는 method는 괄호 안에 숫자를 넣으면 해당 object를 선택하게 됨. eq(0).show()하면 0번째 object를 보이게 해놓고 있으라는 것임

부모요소에 flex를 주고
자식요소에 flex:숫자 를 주면 숫자의 비율만큼 차지하라는 뜻

button, label 두가지는 :before, :after를 가질 수 있다.